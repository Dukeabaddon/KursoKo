/**
 * #hero — fold hero banner + character
 * HeroSection.jsx   — section markup (imports index.css)
 * HeroDecorItem.jsx — single floating decor prop, markup only
 * hero.data.js      — HERO_BANNER_PATH, HERO_DECOR, HERO_CHARACTER, decorImgBase + asset import
 * hero.utils.js     — pure decor style helpers
 * hero.hooks.js     — useHeroSection + useHeroDecorItem motion state
 * index.css         — banner stage, clip-path, glow
 */
export { HeroSection } from './HeroSection.jsx'
